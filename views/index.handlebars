<div class="row">
  {{#each article}} 
    <div class ="col-md-2">
      <div class="card">
          <h4>{{title}}</h4>
          <a href = "/readArticle/{{this._id}}" class="btn blue lighten-1" id="btnSignUp">Article Link</a>
      </div>
    </div>
  {{/each}}
</div>